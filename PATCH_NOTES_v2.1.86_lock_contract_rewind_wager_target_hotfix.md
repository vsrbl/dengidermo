# PATCH NOTES v2.1.86 — LOCK / Contract / Rewind / Wager / Target hotfix

## Исправлено

- Casino `LOCK` теперь снова работает как пользователь описал:
  - первый выпавший `LOCK` выбирает символ и фиксирует свой слот;
  - следующий `LOCK` в этом же или следующем спине копирует уже фиксированный символ;
  - можно накопить 2–3 одинаковых фиксированных слота, например `WPN / WPN / WPN`;
  - приз всё равно выдаётся только за три одинаковых итоговых символа.
- Контрактные призы больше не очищаются сразу после комнаты при входе в INSTALL.
  - Полученные призы снова остаются в HUD.
  - Неиспользованные призы сохраняются, пока не сработают.
  - Использованные призы держатся как used-state до старта следующей комнаты.
- С `REWIND MARK` убраны лишние прямоугольники по центру экрана; оставлен мягкий экранный pulse + текст.
- `TARGET LOCK` получил отдельные звуковые события:
  - при первом захвате цели;
  - при переключении на новую цель;
  - при окончании/сбросе захвата.
- `ROOM WAGER` вынесен из overlay-стека INSTALL и каждый кадр позиционируется рядом с INSTALL panel:
  - ровно справа от окна, по верхней границе;
  - если справа нет места, ставится слева;
  - кнопка теперь находится поверх overlay, принимает hover/click и явно подсвечивается.
- Время INSTALL увеличено в 2 раза:
  - обычный INSTALL: 24 → 48 секунд;
  - boss signature выбор: 32 → 64 секунды;
  - ROOM WAGER accept window: 12 → 24 секунды.

## Проверки

- `node --check` по основным JS-модулям.
- Smoke-test casino LOCK:
  - `LOCK / LOCK / LOCK` превращается в три одинаковых фиксированных символа;
  - новый `LOCK` копирует уже существующий фиксированный символ.
- Проверен `VERSION / BUILD_ID`.
