services:
  - type: web
    name: dengidermo-1
    runtime: node
    plan: free
    buildCommand: npm install --omit=dev --no-audit --no-fund
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: CLIENT_ORIGINS
        value: https://nncckkrr.space,https://vsrbl.github.io,http://localhost:5173,http://127.0.0.1:5173
