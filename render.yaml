services:
  - type: web
    name: dengidermo-1
    env: node
    rootDir: server
    plan: free
    buildCommand: npm install --no-audit --no-fund
    startCommand: npm start
    autoDeploy: true
    healthCheckPath: /health
