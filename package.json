{
  "name": "nncckkrr-space-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for Render when the service is deployed from repository root.",
  "main": "server/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node server/server.js",
    "build": "echo \"static site: no build step\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
