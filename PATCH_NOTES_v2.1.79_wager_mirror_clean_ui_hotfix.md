# v2.1.79 — Wager / Mirror / Clean UI Hotfix

- ROOM WAGER card is now moved into the install overlay layer, aligned to the install panel top edge, and kept clickable above the install panel.
- Removed extra rectangular visual fields from REWIND MARK; it now keeps the tether line and a small anchor cross only.
- MIRROR PAYOUT no longer applies to ordinary INSTALL level-up choices.
- MIRROR PAYOUT now shows per-choice tags in boss/WPN/ABL choice windows: `MIRROR x2` or `UNIQUE`.
- MIRROR PAYOUT now logs what it copied in the event feed.
- MIRROR can copy Q core install/replace by immediately upgrading the resulting Q to the next level.
- MIRROR can copy KILL SWITCH by granting an extra KILL SWITCH charge.
- AEGIS HUD badge now displays only `AEGIS`, not current shield numbers.
- Removed duplicate MIRROR/AEGIS boss signature badge entries.
