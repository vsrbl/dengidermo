# v2.1.191 — R cooldown and Ghost Decoy balance

## Перезарядка R

- `TARGET LOCK`: `12 → 8 с`.
- `REDLINE BOOST`: `14 → 10 с`.
- `GHOST DECOY`: `22 → 15 с`.
- `REWIND MARK`: `20 → 14 с` после возврата или истечения метки.
- Одноразовый `KILL SWITCH` не изменён.

## GHOST DECOY

- Базовая длительность приманки и скрытия удвоена: `4,2 → 8,4 с`.
- Прирост за каждый следующий уровень тоже удвоен: `+1,4 → +2,8 с`.
- Время жизни самой приманки и скрытие героя используют одно значение и заканчиваются синхронно.

## Тексты и проверка

- Описания R показывают новые значения перезарядки.
- Обновлены короткие RU/EN-подсказки призрачной приманки.
- Проверено фактическое применение всех четырёх перезаряжаемых R-способностей.
- Проверены базовая и многоуровневая длительность GHOST DECOY.
- Выполнены синтаксические и регрессионные тесты.
