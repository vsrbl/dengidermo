# v2.1.128 — wager layer / boss reward backdrop hotfix

- Lowered active ROOM WAGER status card to HUD-level layering so chest, casino terminal, install, and boss reward modals render above it.
- Active wager status no longer captures pointer events while modal windows are open.
- Boss signature reward selection now uses the same dark modal backdrop family as the INSTALL window, with a subtle gold threat vignette.
